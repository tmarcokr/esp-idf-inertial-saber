# Implementation Plans

Plans written by the `firmware-architect` subagent (`<short_name>.md`). Plans in this folder are local working documents and are ignored by git (only this README is tracked). Once a plan is fully implemented and merged, move it to `.claude/docs/archive/` and add a line to the archive index.
