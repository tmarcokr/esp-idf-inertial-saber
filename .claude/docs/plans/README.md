# Implementation Plans

Plans written by the `firmware-architect` subagent (`<short_name>.md`). Once a plan is fully implemented and merged, move it to `.claude/docs/archive/` and add a line to the archive index.
