# Archived Plans

Implementation plans that have already been executed. Kept for historical reference only — do **not** execute them again.

| Plan | Outcome |
|:-----|:--------|
| `plan_core_flattening.md` | `main/core/` flattened (`SaberActionBus`, `InertialEffect`, `SaberDataPacket`, `PhysicsConfig`). |
| `plan_engine_refactoring.md` | Engines moved into `main/profiles/inertial/`; `PhysicsConfig` extracted for the bus. |
| `plan_system_refactoring.md` | `HardwareConfig.hpp` moved to `main/system/hardware/`. |

Active plans produced by `firmware-architect` live in `.claude/docs/plans/`.
